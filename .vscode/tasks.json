{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build - PyInstaller",
            "type": "shell",
            "command": "python -c \"print('Starting Build...')\" && ${workspaceFolder}\\venv\\Scripts\\activate.bat && python -m PyInstaller --noconfirm ${workspaceFolder}/mcppt.spec",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}